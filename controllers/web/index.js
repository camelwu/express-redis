const
    home = require('./home'),
    guide = require('./guide'),
    search = require('./search'),
    tags = require('./tags'),
    topic = require('./topic');

module.exports = {
    home,
    guide,
    search,
    tags,
    topic,
}